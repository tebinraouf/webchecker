context t_a { 
    constraint AlertLinkInDivAlert {
        guard : self.hasClass("alert-link")
        check : self.parent.hasClass("alert")
        message : "An a tag with alert-link class should have a parent element with alert and alert-* class."
    }
}