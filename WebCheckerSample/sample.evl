context t_div { 
    constraint DIVWithAlertClass {
        guard : self.class.includes("alert") or self.class.includes("alert-*")
        check : self.hasAttr("role")
        message : "A div element with alert or alert class should have the role attribute defined for assistive technologies."
    }
}